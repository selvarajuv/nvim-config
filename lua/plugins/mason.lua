-- Mason: install and manage LSP servers, linters, formatters, debug adapters.
-- mason-lspconfig is configured alongside nvim-lspconfig in plugins/lsp.lua so
-- the server list lives in one place.

return {
	"williamboman/mason.nvim",
	opts = {},
}
